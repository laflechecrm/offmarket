[build]
  base    = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Variables d'environnement à configurer dans Netlify Dashboard :
# DATABASE_URL      → connexion Neon (postgresql://...@...neon.tech/offmarket?sslmode=require)
# ANTHROPIC_API_KEY → Claude API
